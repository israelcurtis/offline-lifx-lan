services:
  lifx:
    build:
      context: .
      dockerfile: Dockerfile
    network_mode: host
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 3001
      APP_STATE_DIR: /state
      NODE_MAX_OLD_SPACE_SIZE: 80
      MEMORY_WARNING_RSS_MB: 96
    mem_reservation: 80m
    mem_limit: 128m
    pids_limit: 64
    volumes:
      - ./state:/state
